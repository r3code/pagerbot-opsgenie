api_keys:
  slack: "$SLACK_TOKEN"
  pagerduty:
    org: "$PAGERDUTY_ORG"
    key: "$PAGERDUTY_KEY"
  opsgenie:
    org: "$OPSGENIE_ORG"
    key: "$OPSGENIE_KEY"

groups:
  - name: oncall
    schedules:
      - PCLYMC8
